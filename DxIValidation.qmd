---
title: "DxI9000Validation"
format: html
editor: visual
---

```{r, echo=FALSE, warning=FALSE, message=FALSE}
#prepare libraries
library(readxl)
library(data.table)
library(tidyverse)
library(DBI)
library(RSQLite)

```

# Daten einlesen

```{r import valData, warning=FALSE, message=FALSE}
setwd("C:/R_local/autoVal")

# read DXI data
val.dat <- read_excel("C:/R_local/autoVal/240307rawdata.xlsx")
setDT(val.dat)

val.dat$Probennummer <- as.numeric(substr(val.dat$SampleID, 1, nchar(val.dat$SampleID) - 2))
```


```{r transfer valData into SQL db, warning=FALSE, message=FALSE}
# automatically create a SQL TABLE containing DxI9000 data
# function to map R data types to SQLite data types
map.data.types <- function(df) {
  sapply(df, function(column) {
    dtype <- class(column)[1]  # Get the class of the column. If multiple, take the first.
    switch(dtype,
           "factor" = "TEXT",
           "character" = "TEXT",
           "integer" = "INTEGER",
           "numeric" = "REAL",
           "Date" = "TEXT",  # SQLite does not have a DATE type, so dates are often stored as TEXT
           "POSIXct" = "TEXT",  # or as INTEGER (Unix Time)
           "logical" = "INTEGER",  # TRUE/FALSE mapped to 1/0
           "UNKNOWN")  # Default case if type not matched
  })
}

# map data types from val.dat
col.types <- map.data.types(val.dat)

# construct a CREATE TABLE statement for val.dat using the mapped data types
create.table.statement <- paste0("CREATE TABLE IF NOT EXISTS DxIvalData (",
                                paste(
                                  sapply(names(col.types), function(name) paste0('"', name, '"')),
                                  col.types, 
                                  sep=" ", 
                                  collapse=", "),
                                ", ValidationID INTEGER PRIMARY KEY AUTOINCREMENT",
                                ", FOREIGN KEY (Probennummer) REFERENCES MeasurementData (Probennummer)",
                                ")") 

# connect to SQLite DB
con <- dbConnect(SQLite(), dbname = "C:/R_local/labStat/ClinicalChemistry_2.db")

# create the table in the SQLite DB
dbExecute(con, create.table.statement)

# insert val.dat into the SQLite DB
dbWriteTable(con, "DxIvalData", val.dat, append = TRUE, row.names = FALSE)
```


```{r BCI_INLAB tranlation into SQL db, warning=FALSE, message=FALSE}

# import BCI_INLAB data
BCI_INLAB <- fread("C:/R_local/autoVal/query_bez_DxI_result.csv", sep = ";", encoding = "UTF-8")
BCI_INLAB$TestOrderCode <- as.integer(BCI_INLAB$TestOrderCode)

# map data types from val.dat
col.types <- map.data.types(BCI_INLAB)

# construct a CREATE TABLE statement for val.dat using the mapped data types
create.table.statement <- paste0("CREATE TABLE IF NOT EXISTS TranslationData (",
                                paste(
                                  sapply(names(col.types), function(name) paste0('"', name, '"')),
                                  col.types, 
                                  sep=" ", 
                                  collapse=", "),
                                ", TranslationID INTEGER PRIMARY KEY AUTOINCREMENT",
                                ", FOREIGN KEY (Methode) REFERENCES MethodData (Methode)",
                                ", FOREIGN KEY (TestOrderCode) REFERENCES DxIvalData (TestOrderCode)",
                                ")") 

# connect to SQLite DB
con <- dbConnect(SQLite(), dbname = "C:/R_local/labStat/ClinicalChemistry_2.db")

# create the table in the SQLite DB
dbExecute(con, create.table.statement)

# insert val.dat into the SQLite DB
dbWriteTable(con, "TranslationData", BCI_INLAB, append = TRUE, row.names = FALSE)

dbDisconnect(con)
```

```{r, warning=FALSE, message=FALSE}

query.dxi.val <- "SELECT DISTINCT a.Methode, 
                    a.Bezeichnung, 
                    a.Werte, 
                    m.EINHEIT, 
                    t.TestName, 
                    d.DoseResult, 
                    d.DoseUnit, 
                    a.Probennummer, 
                    d.Probennummer  
                  FROM MeasurementData a 
                    JOIN MethodData m ON a.Methode = m.Methode
                    JOIN TranslationData t ON a.Methode = t.Methode
                    JOIN DxIvalData d ON t.TestOrderCode = d.TestOrderCode
                  WHERE a.Bezeichnung = 'Ferritin' AND a.Probennummer = d.Probennummer;"

con <- dbConnect(SQLite(), dbname = "C:/R_local/labStat/ClinicalChemistry_2.db")

query.dxi.val <- dbGetQuery(con, query.dxi.val)

dbDisconnect(con)

# query is further used in DxI_autoValOffcDwnWrd.Rmd

```